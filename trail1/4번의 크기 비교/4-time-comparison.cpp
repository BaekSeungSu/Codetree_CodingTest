#include <iostream>
using namespace std;

int main() {
    int A, B, C, D, E;
    cin >> A >> B >> C >> D >> E;

    cout << (A > B) << '\n';
    cout << (A > C) << '\n';
    cout << (A > D) << '\n';
    cout << (A > E) << '\n';
    return 0;
}